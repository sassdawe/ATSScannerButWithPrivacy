name: Release

on:
  push:
    tags: ['v*']

jobs:
  build:
    strategy:
      matrix:
        include:
          - rid: win-x64
            os: windows-latest
            ext: .exe
          - rid: osx-arm64
            os: macos-latest
            ext: ''
          - rid: osx-x64
            os: macos-latest
            ext: ''
          - rid: linux-x64
            os: ubuntu-latest
            ext: ''
          - rid: linux-arm64
            os: ubuntu-latest
            ext: ''
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Derive version from tag
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Publish
        run: >
          dotnet publish src/AtsScanner.Cli -c Release -r ${{ matrix.rid }}
          --self-contained true
          -p:PublishSingleFile=true
          -p:VersionPrefix=${{ env.VERSION }}
          -o ./dist/${{ matrix.rid }}

      - name: Smoke test executable
        if: matrix.rid != 'linux-arm64'
        shell: bash
        run: |
          ./dist/${{ matrix.rid }}/ats-scanner${{ matrix.ext }} --help

      - name: Package (Unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          cd dist/${{ matrix.rid }}
          zip -r ../../ats-scanner-${{ matrix.rid }}.zip .

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Push-Location "dist/${{ matrix.rid }}"
          Compress-Archive -Path * -DestinationPath "../../ats-scanner-${{ matrix.rid }}.zip" -Force
          Pop-Location

      - uses: actions/upload-artifact@v4
        with:
          name: ats-scanner-${{ matrix.rid }}
          path: ats-scanner-${{ matrix.rid }}.zip

  build-gui:
    strategy:
      fail-fast: false
      matrix:
        include:
          - rid: win-x64
            os: windows-latest
            artifact: gui-win-x64
          - rid: osx-x64
            os: macos-latest
            artifact: gui-osx-x64
          - rid: osx-arm64
            os: macos-latest
            artifact: gui-osx-arm64
          - rid: linux-x64
            os: ubuntu-latest
            artifact: gui-linux-x64
          - rid: linux-arm64
            os: ubuntu-latest
            artifact: gui-linux-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Publish
        run: >
          dotnet publish src/AtsScanner.Gui -c Release -r ${{ matrix.rid }}
          --self-contained true
          -o ./dist/${{ matrix.artifact }}

      - name: Verify published output exists
        shell: bash
        run: |
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            test -f "dist/${{ matrix.artifact }}/AtsScanner.Gui.exe"
          else
            test -f "dist/${{ matrix.artifact }}/AtsScanner.Gui"
          fi

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Push-Location "dist/${{ matrix.artifact }}"
          Compress-Archive -Path * -DestinationPath "../../ats-scanner-${{ matrix.artifact }}.zip" -Force
          Pop-Location

      - name: Package (Unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          cd dist/${{ matrix.artifact }}
          zip -r ../../ats-scanner-${{ matrix.artifact }}.zip .

      - uses: actions/upload-artifact@v4
        with:
          name: ats-scanner-${{ matrix.artifact }}
          path: ats-scanner-${{ matrix.artifact }}.zip

  release:
    needs: [build, build-gui]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
      - uses: softprops/action-gh-release@v2
        with:
          files: '**/*.zip'
          generate_release_notes: true
